export const environment = {
  /**
   * Base URL for the backend API (no trailing slash), e.g. "http://localhost:8000/api/v1"
   * IMPORTANT: Set per environment; do not hardcode in services.
   */
  apiBaseUrl: 'http://localhost:8000/api/v1'
};
